import Player from './Player.js';

class Game {
  constructor() {
    this.game = game;
    this.players = [];
    this.roundCounter = 0;
  }

  getPlayerInfoFromDom() {
    let playerOne = new Player(1, )
  }

}

export default Game;
